# Market Sentinel Dependencies
# Install with: pip install -r requirements.txt

# Async HTTP client
aiohttp>=3.9.0

# Twilio SMS
twilio>=8.10.0

# No other dependencies needed - uses Python stdlib for:
# - asyncio (async runtime)
# - sqlite3 (database)
# - json (config/data)
# - logging (output)
# - dataclasses (models)
# - re (filtering)
# - xml.etree.ElementTree (RSS parsing for news monitor)
