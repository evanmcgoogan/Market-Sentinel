# Market Sentinel Dependencies
# Install with: pip install -r requirements.txt

# Async HTTP client
aiohttp>=3.9.0

# Web dashboard
Flask>=3.0.0
gunicorn>=22.0.0

# Market/LLM helpers used by dashboard features
requests>=2.31.0
yfinance>=0.2.50

# Anthropic SDK — required for Claude headline generation, outlook, and whale intelligence
anthropic>=0.39.0


# No other dependencies needed - uses Python stdlib for:
# - asyncio (async runtime)
# - sqlite3 (database)
# - json (config/data)
# - logging (output)
# - dataclasses (models)
# - re (filtering)
# - xml.etree.ElementTree (RSS parsing for news monitor)
